export const DATE_FORMATS = {
  AU: 'DD/MM/YYYY hh:mm:ss A',
};

export const ISO_DATETIME = 'YYYY-MM-DDTHH:mm:ssZ';

export const DEFAULT_DEV_BRANCH = 'qa'; // 'develop';

export const CONFIG_FOLDER_FROM_HOME_DIR = '.handy';
export const CONFIG_FILE_PATH = `${CONFIG_FOLDER_FROM_HOME_DIR}/config`;

export const DEFAULT_REPO_PATH = 'dev/platform';
