const DATE_FORMATS = {
  AU: 'DD/MM/YYYY hh:mm:ss A',
};

const ISO_DATETIME = 'YYYY-MM-DDTHH:mm:ssZ';

const DEFAULT_DEV_BRANCH = 'qa'; // 'develop';

module.exports = {
  DATE_FORMATS,
  ISO_DATETIME,
  DEFAULT_DEV_BRANCH,
};
